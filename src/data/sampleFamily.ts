import type { FamilyPerson } from '../types/family';

/**
 * Shajira starts empty — add people in the app (edit mode) or import JSON.
 * Kept as an empty fallback when defaultFamily.json is missing/invalid.
 */
export const samplePeople: FamilyPerson[] = [];
